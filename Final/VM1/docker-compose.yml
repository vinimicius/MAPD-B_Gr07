services:
  producer:
    build: .
    volumes:
      - ./:/app
    environment:
      - KAFKA_BOOTSTRAP_SERVERS=10.67.22.134:9092
      - S3_ACCESS_KEY=
      - S3_SECRET_KEY=
      - S3_ENDPOINT_URL=
      - S3_BUCKET_NAME=
